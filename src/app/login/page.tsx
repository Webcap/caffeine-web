import LoginClient from "@/app/login/LoginClient";

export const metadata = {
  title: "Login - Caffeine TV",
  description: "Sign in to your Caffeine TV account to access your personalized streaming experience.",
};

export default function LoginPage() {
  return <LoginClient />;
}
